---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: kafka-burrow
  name: kafka-burrow
spec:
  ports:
  - name: 8000-tcp
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: kafka-burrow
  type: ClusterIP

